var CONFIG = { }
CONFIG["url"] = "/control"
CONFIG["config-url"] = CONFIG["url"] + "/config"
CONFIG["pins-url"] = CONFIG["url"] + "/pins"
CONFIG["rooms"] = {
    "room1":"http://localhost",
    "room2":"http://localhost",
    "google":"http://google.com"
}
