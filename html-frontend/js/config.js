var CONFIG = { }
CONFIG["url"] = "/control"
CONFIG["config-url"] = CONFIG["url"] + "/config"
CONFIG["pins-url"] = CONFIG["url"] + "/pins"
CONFIG["rooms"] = {
    "room1":"localhost",
    "room2":"localhost",
    "google":"google.com"
}
